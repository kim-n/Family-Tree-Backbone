<a class="show-tree" data-tid="<%= tree.id%>" href="#">
  <%= tree.get("name") %>
</a>

<ul class="list-people">  
  <% tree.people().each(function (person) { %>
    <li>
      <a class="show-person" data-pid="<%= person.id%>" href="#">
        <%= person.get("name") %>
      </a>
    </li> 
  <% }) %>
</ul>